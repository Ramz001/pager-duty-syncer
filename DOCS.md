to run this script, you would need docker installed on your machine. Then, you can use the following command to start the syncer:

```bash
docker-compose up -d
```

Also, don't forget add the required environment variables in the `.env` file based on the `.env.example` template.
